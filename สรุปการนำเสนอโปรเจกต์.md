# สรุปโปรเจกต์: Kanban+

เอกสารนี้สรุปสิ่งที่ทำในโปรเจกต์ทั้งหมด สำหรับใช้นำเสนอ

---

## 1. ภาพรวมโปรเจกต์

**Kanban+** คือเว็บแอปจัดการงานแบบ Kanban board คล้าย Trello / Notion / GitHub Projects
พัฒนาด้วย **Next.js (App Router)** + **Prisma ORM** + **PostgreSQL**

เป้าหมาย: ให้ผู้ใช้สร้างบอร์ดงาน แบ่งเป็นคอลัมน์ (List) และการ์ด (Card) ลากจัดลำดับได้ ทำงานร่วมกับทีมผ่านระบบเชิญสมาชิก พร้อมติดตามความคืบหน้าด้วย checklist, label, priority, comment และ activity log
และมีระบบแต้ม/เลเวลเพื่อจูงใจให้ปิดงานตรงเวลา

---

## 2. เทคโนโลยีที่ใช้

| ส่วน | เทคโนโลยี |
|---|---|
| Framework | Next.js 16 (App Router, Server Actions, Turbopack) |
| UI | React 19, Tailwind CSS 4 (ไม่ใช้ component library — เขียนเองทั้งหมด) |
| **ฐานข้อมูล** | **Neon** — ผู้ให้บริการ PostgreSQL แบบ serverless (ข้อมูลจริงอยู่ที่นี่) |
| **ตัวเชื่อมฐานข้อมูล** | **Prisma ORM 7** + `@prisma/adapter-neon` (ไลบรารีในโค้ด ไม่ใช่ฐานข้อมูล) |
| ระบบล็อกอิน | NextAuth v5 (Auth.js) แบบ Credentials + JWT session, แฮชรหัสผ่านด้วย `bcryptjs` |
| Drag & Drop | @dnd-kit (core + sortable) |
| ตรวจสอบข้อมูล | Zod 4 |
| ทดสอบ | Node.js test runner ผ่าน `tsx` (ไม่ได้ติดตั้ง framework เพิ่ม) |

> **จุดที่คนมักเข้าใจผิด:** Prisma กับ Neon เป็นคนละอย่าง
> **Neon** คือเซิร์ฟเวอร์ฐานข้อมูลที่เก็บข้อมูลจริง ส่วน **Prisma ORM** คือไลบรารีในโค้ด
> ที่ช่วยเขียน query แบบ type-safe แทนการเขียน SQL ดิบ
> (Prisma มีบริการฐานข้อมูลของตัวเองชื่อ Prisma Postgres — โปรเจกต์นี้ไม่ได้ใช้)

---

## 3. ฟีเจอร์ที่พัฒนา

### ระบบสมาชิกและความปลอดภัย
- สมัครสมาชิก / เข้าสู่ระบบ / ออกจากระบบ ด้วย **NextAuth v5** (Credentials provider + JWT session)
- ป้องกันเส้นทาง **2 ชั้น** — ชั้นนอกที่ `proxy.ts` (เดิมชื่อ `middleware.ts`) เช็คแบบหยาบว่ามี session ไหม
  ชั้นในทุก Server Action และทุกหน้าเช็คสิทธิ์เองอีกรอบ เพราะชั้นนอกถูกข้ามได้
- **จดจำปลายทางเดิม**: ถ้าโดนเด้งไปล็อกอินระหว่างพยายามเข้าลิงก์เฉพาะ (เช่นลิงก์คำเชิญ) ระบบจะพากลับไปหน้านั้นอัตโนมัติหลังล็อกอินสำเร็จ
- ข้อความตอนล็อกอินพลาดเป็น "อีเมลหรือรหัสผ่านไม่ถูกต้อง" เสมอ ไม่แยกว่าอีเมลผิดหรือรหัสผิด
  เพื่อไม่ให้เดาได้ว่ามีอีเมลนี้อยู่ในระบบ

### บอร์ดและการทำงานร่วมกัน
- สร้างบอร์ดได้หลายบอร์ด ตั้งชื่อ/สี/คำอธิบายได้ พร้อม list เริ่มต้น 3 คอลัมน์ (To Do / Doing / Done) อัตโนมัติ
- เชิญสมาชิกร่วมบอร์ดด้วยอีเมล — สร้างลิงก์คำเชิญแบบมี token, อายุ 7 วัน
- **ระดับสิทธิ์ 3 แบบ** — เจ้าของ (แก้ได้ทุกอย่าง + เชิญคนได้) / แก้ไขได้ / **ดูอย่างเดียว**
  ผู้ที่ดูอย่างเดียวจะไม่เห็นปุ่มแก้ไข และถูกปฏิเสธที่ฝั่งเซิร์ฟเวอร์ด้วย ไม่ใช่แค่ซ่อนปุ่ม
- ตรวจสอบว่าอีเมลที่ล็อกอินตรงกับอีเมลที่ถูกเชิญ ป้องกันการสวมรอยรับคำเชิญ

### List และ Card (CRUD เต็มรูปแบบ)
- สร้าง / แก้ไข (ชื่อ, สี, คำอธิบาย) / ลบ list
- สร้าง / แก้ไข (ชื่อ, รายละเอียด, priority, วันครบกำหนด) / ลบ / ย้ายการ์ดข้าม list
- **ลากสลับลำดับ (Drag & Drop)** ด้วย @dnd-kit ทั้งการ์ดและคอลัมน์ รองรับทั้งเมาส์และคีย์บอร์ด (keyboard sensor เพื่อการเข้าถึง)
- **การสร้าง/แก้ไขทุกอย่างทำผ่าน popup** ไม่มีช่องกรอกลอย ๆ บนหน้าจอ
- ฟิลเตอร์การ์ดตามคำค้น / ผู้รับผิดชอบ / ความสำคัญ / กำหนดส่ง (กรองฝั่ง client ไม่ยิงฐานข้อมูลซ้ำ)

### ฟีเจอร์เสริมของการ์ด
- ป้ายกำกับ (Label) และ Priority ที่กำหนดชื่อ/สีเองได้ต่อบอร์ด
- ผู้รับผิดชอบ (assignee) แบบ toggle เข้า/ออกการ์ด
- Checklist พร้อม progress bar
- ระบบความคิดเห็น (comment) บนการ์ด
- บันทึกกิจกรรมของบอร์ด (activity log) อัตโนมัติทุกการกระทำสำคัญ

### หน้าภาพรวมและการค้นหา
- **Dashboard** — สรุปงานค้าง/เลยกำหนด/เสร็จแล้ว, กราฟงานที่ทำเสร็จราย 7 วัน, ความคืบหน้ารายเดือน, รายการบอร์ดพร้อมวงแหวนความคืบหน้า
- **ค้นหาข้ามบอร์ด** — ค้นชื่อและรายละเอียดการ์ดจากทุกบอร์ดที่มีสิทธิ์เข้าถึง
- **ปฏิทินกำหนดส่ง** — ดูงานทั้งเดือน กรองเฉพาะบอร์ดที่สนใจได้

### ระบบแต้มและกำหนดส่ง (Gamification)
- ทำการ์ดเสร็จ +10 แต้ม ส่งทันกำหนด +5 แต้ม ส่งช้าไม่หักแต้ม
- มีเลเวลและชื่อระดับ (มือใหม่หัดวางแผน → ตำนานส่งงานตรงเวลา) และนับสตรีควันที่ทำงานติดต่อกัน
- **กันฟาร์มแต้มด้วย unique constraint ที่ฐานข้อมูล** — การ์ด 1 ใบให้แต้มแต่ละชนิดได้ครั้งเดียวตลอดชีพ
  ลากเข้า-ออกคอลัมน์ "เสร็จสิ้น" กี่รอบก็ไม่ได้แต้มเพิ่ม จึงไม่ต้องล็อกการ์ดที่เสร็จแล้ว
- แต้มทั้งหมดคำนวณสดจากตาราง `PointEvent` ซึ่งเป็นแหล่งความจริงเดียว ไม่มีตารางเก็บยอดรวมให้ไม่ตรงกัน

---

## 4. ขั้นตอนการพัฒนา (Timeline)

| วันที่ | สิ่งที่ทำ |
|---|---|
| 5 ส.ค. 2569 | เริ่มโปรเจกต์จาก Create Next App |
| 5 ส.ค. 2569 | สร้างระบบหลัก: auth, boards, checklists, comments, labels, invites |
| 5 ส.ค. 2569 | แก้ให้ redirect กลับหน้าเดิมได้หลัง login/signup |
| 5 ส.ค. 2569 | เพิ่มลากสลับลำดับการ์ดด้วย @dnd-kit |
| 6 ส.ค. 2569 | เพิ่ม CRUD ของ list/card เต็มรูปแบบ, ลากสลับลำดับคอลัมน์, ระบบ Priority ต่อบอร์ด |
| 6 ส.ค. 2569 | เขียน README ให้ครบตั้งแต่ clone จนรันได้จริง |
| 7 ส.ค. 2569 | ทดสอบทุกฟีเจอร์จริงบนเบราว์เซอร์ + เขียนคู่มือการใช้งาน + พบและบันทึกบั๊กลากคอลัมน์ |
| 28 ส.ค. 2569 | ครอบ `<Suspense>` ให้ฟอร์ม login/signup ไม่งั้น production build พัง |
| 28 ส.ค. 2569 | **เปลี่ยนระบบ session ที่เขียนเองด้วย `jose` ไปใช้ NextAuth v5** |
| 28 ส.ค. 2569 | เขียน `CLAUDE.md` เป็นเอกสารสถาปัตยกรรมของโปรเจกต์ |
| 28 ส.ค. 2569 | ตั้ง priority ของการ์ดได้จากตัวการ์ดโดยตรง |
| 2 ก.ย. 2569 | ทำโครงแอปใหม่ (sidebar + topbar), dashboard, ค้นหา, ปฏิทิน, ระบบแต้ม, กำหนดส่ง |
| 2 ก.ย. 2569 | **เขียน automated test ชุดแรก** ครอบตรรกะวันที่และแต้ม |
| 2 ก.ย. 2569 | ย้ายการสร้าง/แก้ไขทุกอย่างไปเป็น popup + **ทำระบบสิทธิ์ EDITOR/VIEWER ให้ใช้งานได้จริง** |
| 2 ก.ย. 2569 | เคลียร์หนี้ทางเทคนิค 3 จุด (ประสิทธิภาพ query, ตัวเลขที่ไม่ตรงกัน, regex ที่เขียนผิด) |
| 2 ก.ย. 2569 | **แก้บั๊กลากคอลัมน์ที่ค้างมาตั้งแต่ 7 ส.ค.** พร้อมเขียน test กันไม่ให้กลับมา |

---

## 5. ผลการทดสอบ

### ทดสอบด้วยมือบนเบราว์เซอร์
(ดูรายละเอียดที่ [คู่มือการใช้งาน.md](./คู่มือการใช้งาน.md))

| ฟีเจอร์ | สถานะ |
|---|---|
| สมัครสมาชิก / เข้าสู่ระบบ / ออกจากระบบ | ✅ ใช้งานได้ปกติ |
| จดจำหน้าเดิมหลังล็อกอิน | ✅ ใช้งานได้ปกติ |
| สร้าง/แสดงบอร์ด | ✅ ใช้งานได้ปกติ |
| สร้าง/แก้ไข/ลบ list | ✅ ใช้งานได้ปกติ |
| ลากสลับลำดับการ์ด | ✅ ใช้งานได้ปกติ |
| **ลากสลับลำดับ list** | ✅ **แก้แล้ว** (เคยเป็นบั๊ก — ดูด้านล่าง) |
| สร้าง/แก้ไข/ลบ/ย้ายการ์ด | ✅ ใช้งานได้ปกติ |
| ป้ายกำกับ + Priority | ✅ ใช้งานได้ปกติ |
| ผู้รับผิดชอบการ์ด | ✅ ใช้งานได้ปกติ |
| Checklist | ✅ ใช้งานได้ปกติ |
| ความคิดเห็น | ✅ ใช้งานได้ปกติ |
| Activity log | ✅ ใช้งานได้ปกติ |
| เชิญสมาชิก + ตรวจสอบอีเมล | ✅ ใช้งานได้ปกติ |
| สิทธิ์ "ดูอย่างเดียว" | ✅ ใช้งานได้ปกติ |
| ระบบแต้ม / เลเวล / สตรีค | ✅ ใช้งานได้ปกติ |

### ทดสอบอัตโนมัติ (Automated test)

รันด้วย `npm test` — **34 เคส ผ่านทั้งหมด** ใช้ test runner ที่มากับ Node.js
ไม่ได้ติดตั้งเครื่องมือเพิ่ม

| ชุดทดสอบ | ครอบอะไร |
|---|---|
| `lib/due.test.ts` | เทียบวันกำหนดส่งตามเวลาไทย, จัดกลุ่มเลยกำหนด/วันนี้/ภายใน 7 วัน, ตัดสินว่าส่งทันไหม |
| `lib/points.test.ts` | สูตรเลเวลจากแต้ม, การนับสตรีควันติดต่อกัน |
| `lib/drag.test.ts` | ตรรกะตัดสินตำแหน่งตอนลากวาง (รวมเคสของบั๊กที่แก้ไป) |

### บั๊กที่พบและวิธีแก้

**อาการ:** ลาก **list** (คอลัมน์) เพื่อสลับตำแหน่ง แล้วคอลัมน์เด้งกลับที่เดิมหลังปล่อยเมาส์
ทั้งที่ระหว่างลากเห็น preview ว่าย้ายแล้ว ส่วนการลากการ์ดทำงานถูกต้อง

**สาเหตุ:** @dnd-kit ใช้วิธีตรวจจับแบบ `closestCorners` ซึ่งเลือก "สิ่งที่อยู่ใกล้ที่สุด"
เป็นเป้าหมายที่ปล่อย — เวลาลากคอลัมน์ไปทับคอลัมน์อื่นที่มีการ์ดอยู่ สิ่งที่ใกล้ที่สุด
มักเป็น**การ์ด**ไม่ใช่ตัวคอลัมน์ โค้ดเดิมเอา id ที่ได้ไปหาในรายการคอลัมน์ตรง ๆ จึงหาไม่เจอ
แล้วออกจากฟังก์ชันไปเงียบ ๆ ตำแหน่งใหม่เลยไม่ถูกบันทึก

```
เดิม:  หา "การ์ด b2" ในรายการคอลัมน์ → ไม่เจอ → ยกเลิกการย้าย
ใหม่:  หา "การ์ด b2" ในรายการคอลัมน์ → ไม่เจอ
       → หาต่อว่าการ์ดนี้อยู่คอลัมน์ไหน → เจอ "คอลัมน์ B" → ย้ายไปคอลัมน์ B
```

**วิธีแก้:** แยกตรรกะการตัดสินใจออกมาเป็นฟังก์ชันบริสุทธิ์ที่ `lib/drag.ts` แล้วให้มัน
แปลง id ของการ์ดกลับเป็นคอลัมน์ที่การ์ดนั้นอยู่ก่อนคำนวณตำแหน่ง

**วิธีพิสูจน์ว่าแก้ได้จริง:** เขียนเทสต์ก่อน แล้วรันกับโค้ดเดิมให้เห็นว่า**แดง**เฉพาะเคสของบั๊ก
(เคสอื่นเขียวหมด แปลว่าเทสต์ไม่ได้เขียนหลวม) จากนั้นจึงแก้โค้ดให้เขียว

---

## 6. จุดที่เรียนรู้ระหว่างทาง

**เรื่องเขตเวลา** — `dueDate` ถูกเก็บเป็นเที่ยงคืน UTC แต่ไทยเป็น UTC+7 ถ้าเทียบกับเวลาปัจจุบัน
ตรง ๆ จะเพี้ยนข้ามวัน (ส่งงานตอนตีหนึ่งจะถูกนับเป็นวันก่อนหน้า) แก้ด้วยการเทียบเป็น
"คีย์วันที่" แบบ `YYYY-MM-DD` ตามเวลาไทยเสมอ และรวมไว้ที่ไฟล์เดียว

**เรื่องความถูกต้องของข้อมูล** — กันการฟาร์มแต้มด้วย unique constraint ที่ระดับฐานข้อมูล
แทนการเช็คในโค้ด เพราะถ้าผู้ใช้กดพร้อมกันสองแท็บ การเช็คในโค้ดจะหลุด แต่ฐานข้อมูลกันได้แน่นอน

**เรื่องความปลอดภัย** — โครงสร้างฐานข้อมูลมีช่อง "ระดับสิทธิ์" มาตั้งแต่แรก แต่ไม่เคยมีโค้ดตรงไหน
อ่านค่านั้นเลย เท่ากับสิทธิ์ "ดูอย่างเดียว" ไม่มีผลจริง แก้โดยให้ทุก action ที่แก้ข้อมูล
ตรวจสิทธิ์ก่อน และพิสูจน์ด้วยการยิงคำสั่งตรงข้ามหน้าจอไปที่เซิร์ฟเวอร์แล้วดูว่าถูกปฏิเสธจริง

**เรื่อง deploy** — โค้ดที่ Prisma สร้างอัตโนมัติถูกตั้งไม่ให้ขึ้น git (ถูกต้องแล้ว) แต่ถ้าไม่สั่ง
`prisma generate` ในขั้นตอน build เซิร์ฟเวอร์ที่ deploy จะหาไฟล์ไม่เจอและ build พัง —
บั๊กประเภทนี้ไม่โผล่ตอนพัฒนาในเครื่องเลย เพราะไฟล์ถูกสร้างไว้แล้ว

---

## 7. เอกสารประกอบ

- [README.md](./README.md) — วิธีติดตั้งและรันโปรเจกต์
- [คู่มือการใช้งาน.md](./คู่มือการใช้งาน.md) — วิธีใช้งานแต่ละฟีเจอร์แบบละเอียด พร้อมผลทดสอบ
- [CLAUDE.md](./CLAUDE.md) — เอกสารสถาปัตยกรรม อธิบายว่าโค้ดแต่ละส่วนอยู่ที่ไหนและทำไมถึงออกแบบแบบนั้น
