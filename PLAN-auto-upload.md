# แผน: สร้างการ์ดให้ครบในครั้งเดียว + อัปโหลดไฟล์แนบอัตโนมัติ

สถานะ: ทำฟีเจอร์แล้วและผ่านการตรวจหลัก (17 กันยายน 2569) — เคสที่ยังไม่ทดสอบจริงระบุไว้ด้านล่าง
ขอบเขตที่ตกลงกันแล้ว: **ทางเลือก B** (อัปทันที + เลือกได้หลายไฟล์) และ **ไม่ทำ progress bar เป็น %**
ขอบเขตเพิ่มเติม: หน้าสร้างการ์ดใช้หน้าตาและช่องกรอกเหมือนหน้ารายละเอียดการ์ดตามภาพอ้างอิง เพื่อให้ผู้ใช้กรอกข้อมูลครบในการสร้างครั้งเดียว
ไฟล์นี้เป็นเอกสารชั่วคราวสำหรับรีวิวแผน ลบทิ้งได้หลังทำเสร็จ (ไม่ต้อง commit)

## ผลการลงมือทำ

- หน้าสร้างการ์ดขนาดใหญ่ พร้อม dropdown ร่วมกับหน้ารายละเอียด เช็กลิสต์ ลิงก์ ไฟล์ และความคิดเห็นเริ่มต้น
- สร้างข้อมูลทั้งหมดใน transaction เดียว มี validation และตรวจสิทธิ์/ข้อมูลอ้างอิงภายในบอร์ด
- ใช้ request ID เดิมเมื่อลองสร้างซ้ำหลังเครือข่ายขัดข้อง และ attachment ID แยกรายไฟล์เพื่อรองรับชื่อซ้ำ
- คิวไฟล์ร่วมใน `attachment-upload.tsx` เก็บภาพตัวอย่างไว้ก่อนสร้าง แล้วอัปโหลดอัตโนมัติทีละใบ
  การ์ดเดิมอัปทันทีที่เลือก ข้อผิดพลาดแสดงรายไฟล์และลองใหม่ได้
- ป้องกันการปิดหน้าสร้างระหว่างบันทึก และคืน focus ไปปุ่ม dropdown เมื่อกด Esc
- ตรวจ TypeScript, lint, unit tests 48 เคส และ production build ผ่านแล้ว
  การรันทดสอบและ build ใช้สิทธิ์นอก sandbox เนื่องจากข้อจำกัด subprocess/การโหลด Google Fonts
- ไม่แก้ `.env.example` ที่ถูกลบค้างไว้ก่อนเริ่มงาน
- ทดสอบผ่านเบราว์เซอร์จริง: สร้างพร้อมรายละเอียด เช็กลิสต์ที่ติ๊กแล้ว ผู้รับผิดชอบ
  ความคิดเห็น ลิงก์ และไฟล์สองใบ จากนั้นเปิดรายละเอียดตรวจครบและตรวจข้อมูลในฐานข้อมูลซ้ำ
- ทดสอบแนบไฟล์ชื่อเดิมในการ์ดที่มีอยู่: อัปอัตโนมัติและเป็นคนละไฟล์กับใบเดิม
- ตรวจจอมือถือ 390 × 844, Escape ปิดเฉพาะ dropdown และคืน focus,
  ยกเลิกก่อนสร้างแล้วไม่มีการ์ดเพิ่ม, ปฏิเสธไฟล์ว่าง/เกินขนาด
- ล้างการ์ดทดสอบ กิจกรรม และไฟล์ Blob ทั้งสามที่ใช้ทดสอบเรียบร้อย
- ยังไม่จำลองจริง: เครือข่ายขัดข้องระหว่างอัปโหลด/การลองใหม่หลังสำเร็จบางใบ,
  กด Esc ในช่วงที่เซิร์ฟเวอร์ยังทำงาน, บัญชี VIEWER และการส่ง ID ข้ามบอร์ด
  ส่วนนี้มีการจัดการในโค้ดและตรวจทานแล้ว แต่ไม่ถือว่าผ่าน browser integration test

รายละเอียดด้านล่างเป็นแผนต้นทาง โดย implementation ใช้รายการไฟล์ที่มี ID, สถานะ และ error
แทน array ชื่อไฟล์อย่างเดียว เพื่อให้จัดการชื่อซ้ำและ retry ได้ถูกต้อง

---

## ปัญหาตอนนี้

`app/(app)/board/[id]/card-attachments.tsx` แนบไฟล์ต้องกด 2 จังหวะ — เลือกไฟล์จาก
`<input type="file">` แล้วค่อยกดปุ่ม "อัปโหลด" ที่ยิง `uploadAttachmentAction` ผ่าน `<form>`
ผู้ใช้หลายคนเลือกไฟล์แล้วคิดว่าจบแล้ว จึงลืมกดปุ่ม

## เป้าหมาย

เลือกไฟล์แล้วอัปเลย ไม่มีปุ่มยืนยัน และเลือกทีเดียวได้หลายไฟล์

หน้าสร้างการ์ดต้องกรอกชื่อ รายละเอียด กำหนดส่ง เช็กลิสต์ ไฟล์/ลิงก์แนบ ระดับความสำคัญ
ป้ายกำกับ และผู้รับผิดชอบได้ครบ โดยไม่ต้องสร้างก่อนแล้วเปิดรายละเอียดเพื่อเติมข้อมูล

## หน้าสร้างการ์ดแบบเดียวกับรายละเอียดการ์ด

### หน้าตาและพฤติกรรม

- เปลี่ยน `card-create-dialog.tsx` จาก modal ขนาดเล็กเป็นขนาดใหญ่เหมือน `card-detail-dialog.tsx`
  ใช้สองคอลัมน์บนจอใหญ่และคอลัมน์เดียวบนมือถือ พร้อมระยะห่าง สี และ footer แบบเดียวกับภาพ
- ฝั่งซ้าย: ชื่อการ์ด รายละเอียด กำหนดส่ง เช็กลิสต์ที่เพิ่ม/ลบรายการได้ ไฟล์และลิงก์แนบ
  และช่องความคิดเห็นเริ่มต้นแบบไม่บังคับ เพื่อรองรับการกรอกในครั้งเดียว
- ฝั่งขวา: แสดงคอลัมน์ปลายทาง และ dropdown ระดับความสำคัญ ป้ายกำกับ ผู้รับผิดชอบ
  โดย priority เลือกหนึ่งค่า ส่วนป้ายและผู้รับผิดชอบเลือกหลายค่า เมนูสมาชิกแสดงชื่อและอีเมล
- Footer ใช้ปุ่มหลัก “เพิ่มการ์ด” และปุ่ม “ยกเลิก” ไม่แสดงปุ่มลบหรือย้ายการ์ดก่อนสร้าง
  สถานะและกำหนดส่งที่แสดงต้องคำนวณจากข้อมูลจริง ไม่คัดลอกค่าตัวอย่างในภาพ
- กรอกแล้วกดเพิ่มการ์ดครั้งเดียว ข้อมูลทุกส่วนต้องถูกบันทึกโดยไม่ต้องเปิดรายละเอียดซ้ำ

### ไฟล์แนบก่อนมี cardId

แนวทางในแผนนี้: ก่อนกด “เพิ่มการ์ด” เก็บไฟล์ที่เลือกไว้ในหน่วยความจำ พร้อมชื่อ ขนาด
และภาพตัวอย่าง โดยแสดงสถานะ “รอสร้างการ์ด” ให้ชัดเจน ยังไม่อัปโหลดและไม่สร้างการ์ดเปล่าเบื้องหลัง
เมื่อกดเพิ่มการ์ดและได้ `cardId` แล้ว จึงอัปโหลดไฟล์ทีละใบอัตโนมัติ ไม่ต้องกดอัปโหลดเพิ่ม
ส่วนการ์ดที่มีอยู่แล้วให้อัปทันทีที่เลือกตามแผนเดิมด้านล่าง

- ยกเลิกก่อนสร้าง: ทิ้งข้อมูลและไฟล์ในหน่วยความจำ พร้อมคืนทรัพยากรภาพตัวอย่าง ไม่มีการ์ดหรือไฟล์ค้างบนเซิร์ฟเวอร์
- บันทึกข้อมูลหลักไม่ผ่าน: คงหน้าต่างและค่าที่กรอก พร้อมแสดงข้อผิดพลาด
- สร้างสำเร็จแต่อัปโหลดบางไฟล์ไม่ผ่าน: คง `cardId` และรายการไฟล์ที่ล้มเหลวไว้
  แจ้งว่าการ์ดสร้างแล้ว และให้ลองเฉพาะไฟล์ที่ล้มเหลวอีกครั้งโดยไม่สร้างการ์ดซ้ำ
- ระหว่างบันทึก/อัปโหลดป้องกันการกดซ้ำและปิด modal โดยไม่ตั้งใจ รวมทั้ง Esc และคลิกฉากหลัง
  หลังจบทั้งหมดจึงปิดหน้าต่าง หากมีข้อผิดพลาดให้เลือกลองใหม่หรือปิดพร้อมเก็บการ์ดที่สร้างแล้ว

### งานที่ต้องปรับเพิ่ม

1. ส่งป้ายกำกับและสมาชิกของบอร์ดจาก `kanban-board.tsx` ไปยังหน้าสร้างการ์ดเพิ่มเติมจาก priorities
2. แยกส่วนหน้าตาและตัวเลือกที่ใช้ร่วมกันระหว่างหน้าสร้างและหน้ารายละเอียด
   โดยโหมดสร้างเก็บค่าในฟอร์ม ส่วนโหมดแก้ไขทำงานกับการ์ดที่มีอยู่ อย่าเรียก action ที่ต้องมี `cardId` ก่อนสร้าง
3. ขยาย `createCardAction` ให้รับข้อมูลทุกส่วนและคืนผลสำเร็จพร้อม `cardId` หรือข้อผิดพลาด
   ตรวจสิทธิ์และตรวจว่าคอลัมน์ priority ป้าย และสมาชิกเป็นของบอร์ดนี้บนเซิร์ฟเวอร์
4. บันทึกการ์ด ความสัมพันธ์ เช็กลิสต์ ลิงก์ ความคิดเห็นเริ่มต้น (ถ้ามี) และกิจกรรมสร้าง
   ภายใน transaction เดียว ส่วนไฟล์อัปโหลดตามหลังและจัดการความล้มเหลวแยกรายไฟล์
5. ใช้รหัสชั่วคราวแยกรายการไฟล์ ไม่ใช้ชื่อไฟล์เป็น key เพราะเลือกไฟล์ชื่อซ้ำได้
   จัดการ exception จากเครือข่ายและเซิร์ฟเวอร์ พร้อมคืนสถานะ pending เสมอ
6. อัปเดต `CLAUDE.md` ให้บอก flow สร้างแบบครบในครั้งเดียว และความต่างของไฟล์รอสร้างกับไฟล์ที่อัปทันที
   อ่านคู่มือ Next.js ใน `node_modules/next/dist/docs/` ที่เกี่ยวข้องก่อนเริ่มแก้โค้ดตาม `AGENTS.md`

### เกณฑ์ตรวจรับเพิ่มเติม

- [x] หน้าสร้างและหน้ารายละเอียดมีโครงและ dropdown สอดคล้องกัน ทั้งจอใหญ่และมือถือ
- [ ] สร้างพร้อมทุกช่องแล้วเปิดรายละเอียดพบข้อมูลครบ ถูกคอลัมน์ และมีการ์ดเพียงใบเดียว
- [ ] สร้างด้วยชื่ออย่างเดียวได้ และยกเลิกก่อนสร้างแล้วไม่มีข้อมูลหรือไฟล์ค้าง
- [ ] ไฟล์หลายใบ/ชื่อซ้ำ/ขนาดเกิน/ไฟล์ว่างแสดงสถานะแยกรายไฟล์ได้ถูกต้อง
- [ ] อัปโหลดล้มเหลวบางใบแล้วลองใหม่ได้โดยไม่สร้างการ์ดหรืออัปโหลดไฟล์ที่สำเร็จซ้ำ
- [ ] ข้อผิดพลาดไม่ทำให้ค่าที่กรอกหาย และกดเพิ่มซ้ำระหว่างรอไม่ได้
- [x] ใช้งานด้วยคีย์บอร์ดได้ มี focus ที่มองเห็น และปิด dropdown ด้วย Esc ไม่ปิดทั้ง modal
- [ ] VIEWER สร้างไม่ได้ และข้อมูลอ้างอิงข้ามบอร์ดถูกปฏิเสธบนเซิร์ฟเวอร์

---

## ข้อจำกัดที่ต้องเคารพ (อย่าลืมตอนเขียน)

1. **`serverActions.bodySizeLimit` = 4.4mb ใน `next.config.ts` เป็นลิมิตต่อ request**
   ไฟล์ 3MB สองใบในก้อนเดียวจะเด้ง → **ต้องวนยิงทีละไฟล์ ห้ามรวมเป็น FormData ก้อนเดียว**
   ผลพลอยได้คือไฟล์ทยอยขึ้นทีละใบ ผู้ใช้เห็นความคืบหน้า
2. **ไม่มี progress bar เป็น %** เพราะไฟล์วิ่งผ่านเซิร์ฟเวอร์ Next ก่อนค่อยไป Vercel Blob
   ถ้าจะเอา % จริงต้องใช้ client upload ของ `@vercel/blob` ซึ่งต้องมี API route
   = ขัดกฎ "ไม่มี API route นอกจาก NextAuth" ใน CLAUDE.md — เจ้าของโปรเจกต์ตัดสินใจว่าไม่เอา
3. **โปรเจกต์นี้ไม่มี spinner icon** ที่อื่นใช้ข้อความ pending ล้วน (`pendingLabel` ของ `SubmitButton`)
   → ตามนั้น ห้ามเพิ่มไอคอนหมุนใหม่
4. ห้ามเพิ่ม dependency ห้ามฮาร์ดโค้ดสี ใช้ token จาก `globals.css` เท่านั้น
5. `MAX_ATTACHMENT_BYTES` = 4MB อยู่ที่ `lib/attachments.ts` **ห้ามฮาร์ดโค้ดซ้ำ** import มาใช้

---

## ขั้นตอน

### 1. `app/(app)/board/[id]/actions.ts` — ให้ `uploadAttachmentAction` คืนผลลัพธ์

ตอนนี้พลาดแล้ว `return` เงียบทุกกรณี พอเป็นออโต้อัปโหลดจะแย่กว่าเดิม — ถ้าไม่มี
`BLOB_READ_WRITE_TOKEN` ผู้ใช้จะเห็นแค่ "กำลังอัปโหลด..." แล้วไฟล์หายไปเฉย ๆ

เปลี่ยน return type เป็น:

```ts
type UploadResult = { ok: true } | { ok: false; error: string };
```

- input ไม่ถูกต้อง → `{ ok: false, error: "ไฟล์ไม่ถูกต้อง" }`
- เกินขนาด → `{ ok: false, error: "ไฟล์ใหญ่เกิน 4 MB" }` (ประกอบข้อความจาก `formatFileSize`)
- ไม่มี token → `{ ok: false, error: "ระบบยังไม่ได้ตั้งค่าที่เก็บไฟล์" }`
- ไม่มีสิทธิ์ → `{ ok: false, error: "ไม่มีสิทธิ์แก้ไขการ์ดนี้" }`
- สำเร็จ → `{ ok: true }` (หลัง `revalidatePath` เหมือนเดิม)

ไม่ขัดกฎ CLAUDE.md เพราะข้อนั้นบอกให้ return เงียบเฉพาะ "action ที่ไม่มี UI error"
ซึ่งอันนี้มี UI error อยู่แล้ว (`sizeError`) — **ต้องอัปเดตบรรทัดในหัวข้อ Environment variables
ของ CLAUDE.md ที่เขียนว่า "action คืนเงียบ ๆ" ด้วย**

ส่วนอัปโหลดแก้ action นี้ ส่วนการสร้างขยาย `createCardAction` ตามขอบเขตเพิ่มเติมด้านบน

### 2. `card-attachments.tsx` — เปลี่ยนปุ่มเป็น label + อัปเองใน onChange

**ปุ่ม**
- ลบ `<form action={uploadAttachmentAction}>` ของไฟล์ทิ้ง (ช่องแนบลิงก์คงเดิมทุกบรรทัด)
- แทนด้วย `<label>` หน้าตาเดิม (`ghostButtonClass`) ครอบ `<input type="file" multiple className="sr-only">`
  ได้ปุ่มสวย ไม่มี "No file chosen" ค้าง และยัง focus ด้วยคีย์บอร์ดได้
- ข้อความปุ่ม: `<IconImage size={13} /> แนบไฟล์`
- ระหว่างอัปโหลด → `pointer-events-none opacity-60` กันกดซ้อน

**state**
```ts
const [uploading, setUploading] = useState<string[]>([]);  // ชื่อไฟล์ที่กำลังอัป
const [errors, setErrors] = useState<string[]>([]);        // ขยายจาก sizeError เดิม (string เดียว)
const [isPending, startTransition] = useTransition();
```

**onChange**
1. อ่าน `Array.from(event.currentTarget.files ?? [])`
2. แยกไฟล์ที่ `!isWithinAttachmentSizeLimit(file.size)` ออกก่อน → เข้า `errors` ทันที (ด่านแรก
   เหมือน `checkSize()` เดิม ด่านจริงยังอยู่ใน action)
3. `event.currentTarget.value = ""` **ทำก่อน await** ไม่งั้นเลือกไฟล์เดิมซ้ำแล้ว `onChange` ไม่ยิง
   โดยเก็บ input และรายการไฟล์ที่ต้องใช้ก่อนเริ่มงาน async
4. `startTransition(async () => { ... })` — React 19 รับ async callback ได้
   วน `for (const file of ok)`:
   - ประกอบ `FormData` ใส่ `cardId` + `file`
   - `const result = await uploadAttachmentAction(fd)`
   - ถ้า `!result.ok` → ต่อ `${file.name}: ${result.error}` เข้า `errors`
   - ตัดชื่อไฟล์ออกจาก `uploading` ทีละใบ
5. ล้าง `errors` ตอนเริ่มรอบใหม่

**placeholder ระหว่างรอ**
- แถวใต้ลิสต์ `others`: ไอคอน `IconFile` + ชื่อไฟล์ + "กำลังอัปโหลด..." คลุมด้วย `animate-pulse`
  สีข้อความ `text-muted`
- หายไปทีละแถวเมื่อแต่ละไฟล์เสร็จ (ของจริงจะโผล่มาจาก `revalidatePath` แทน)

**error**
- แสดงเป็นลิสต์ `text-danger text-[11px]` ทีละบรรทัด (เดิมเป็น `<p>` เดียว)

### 3. `CLAUDE.md`

- หัวข้อหน้าบอร์ด: เพิ่มว่าไฟล์แนบอัปทันทีที่เลือก **ห้ามเอาปุ่ม "อัปโหลด" กลับมา**
  และเลือกหลายไฟล์ได้แต่**ต้องยิงทีละไฟล์**เพราะ `bodySizeLimit` เป็นลิมิตต่อ request
- หัวข้อ Environment variables: แก้บรรทัด `BLOB_READ_WRITE_TOKEN` ที่เขียนว่า "action คืนเงียบ ๆ"
  → ตอนนี้คืน error ให้ผู้ใช้เห็นแล้ว

### 4. ตรวจสอบ

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm run build`
- [ ] ลองจริง: เลือกไฟล์เดียว / เลือก 3 ไฟล์พร้อมกัน / ไฟล์เกิน 4MB / เลือกไฟล์ชื่อเดิมซ้ำสองรอบ
- [ ] ลองกด Esc ระหว่างอัปโหลด — `<dialog>` ปิดแล้ว action ที่ค้างอยู่ต้องไม่ error ค้าง
- [ ] เช็คด้วยบัญชี VIEWER ว่าปุ่มไม่โผล่ (prop `canEdit`) และถึงยิง action ตรงก็ต้องโดนปฏิเสธ

---

## ขอบเขตที่คงเดิม

พฤติกรรมแนบลิงก์และลบไฟล์ของการ์ดที่มีอยู่ · `lib/attachments.ts` · `prisma/schema.prisma` · `next.config.ts`
· การ์ดบนคอลัมน์ (`board-card.tsx`) ที่นับจำนวนไฟล์แนบ

## เก็บไว้ทำทีหลังถ้าอยาก (ทางเลือก C)

ลากไฟล์มาวางบนกล่องไฟล์แนบ (`onDrop`) และ Ctrl+V รูปที่แคปไว้ (`onPaste`) — เป็น native event
ล้วน ไม่ต้องลงไลบรารี ใช้ฟังก์ชัน `handleFiles()` ตัวเดียวกับข้อ 2 ต่อยอดได้เลย
